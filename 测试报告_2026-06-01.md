# 匿名社交平台 P0 测试报告

## 1. 测试概况

| 项目 | 内容 |
|---|---|
| 测试日期 | 2026-06-01 |
| 测试环境 | 本地代码审查 |
| 测试方式 | 静态代码分析 + 接口对齐检查 |
| 执行者 | AI Agent |
| 总用例数 | 12 (P0) |
| 通过 | 6 |
| 失败/问题 | 4 |
| 阻塞 | 2 |
| 未实现 | 0 |

## 2. 测试结论

**整体结论**：P0 核心流程后端实现较为完整，但 App 端存在若干接口缺失和编译问题，需要修复后才能进行端到端测试。

**是否建议上线**：否，需先修复以下问题。

**主要风险**：
1. App 端存在编译错误（缺少组件导入）
2. 部分 API 前后端未对齐
3. 高并发场景下存在数据一致性问题

---

## 3. 详细测试结果

### TC-AUTH-001/002/003/004: 注册登录 ✅ Pass

| 检查项 | 结果 |
|---|---|
| 后端注册接口 | ✅ 实现完整，含密码哈希、昵称生成、重复检查 |
| 后端登录接口 | ✅ 实现完整，含 Token 生成、状态校验 |
| 后端刷新接口 | ✅ 实现完整 |
| App 注册页 | ✅ 有协议勾选、表单校验 |
| App 登录页 | ✅ 已实现 |
| **问题** | 注册后未自动登录（与文档一致，已备注） |

---

### TC-CATEGORY-001/002: 分区浏览 ✅ Pass

| 检查项 | 结果 |
|---|---|
| 后端分区列表 | ✅ `/categories` GET |
| 后端分区详情 | ✅ `/categories/:id` GET |
| 后端分区帖子 | ✅ `/categories/:id/posts` GET |
| 后端负责人列表 | ✅ `/categories/:id/moderators` GET |
| App 分区列表页 | ✅ CategoryScreen 已实现 |
| App 分区详情页 | ✅ CategoryDetailScreen 已实现（含公告/规则/负责人/帖子流） |
| 管理入口 | ✅ 负责人可见齿轮图标，点击进入 ModeratorPanel |

---

### TC-POST-001/002/003: 发帖 ✅ Pass

| 检查项 | 结果 |
|---|---|
| 后端创建帖子 | ✅ `/posts` POST，含分区存在校验 |
| 后端帖子详情 | ✅ `/posts/:id` GET |
| 后端删除帖子 | ✅ `/posts/:id` DELETE，含权限校验（只能删自己的） |
| App 发布页 | ✅ PublishScreen 已实现，有空内容和分区校验 |
| **问题** | 图片选择为占位 UI，无实际功能（Blocked，不在 P0） |

---

### TC-LIKE-001: 点赞 ⚠️ 部分通过

| 检查项 | 结果 |
|---|---|
| 后端点赞接口 | ✅ `/posts/:id/like` POST/DELETE |
| 帖子详情页点赞 | ✅ PostDetailScreen 已实现 handleLike |
| **问题** | PostCard（信息流卡片）点赞按钮未绑定点击事件，仅展示 UI |
| 建议 | 为 PostCard 的点赞按钮添加 onPress 处理 |

---

### TC-COMMENT-001/002: 评论 ❌ 失败

| 检查项 | 结果 |
|---|---|
| 后端评论列表 | ✅ `/posts/:id/comments` GET |
| 后端发表评论 | ✅ `/posts/:id/comments` POST |
| 后端删除评论 | ✅ `/comments/:id` DELETE |
| **问题 1** | App 端 **缺少 comment.ts API 文件**，PostDetailScreen 无法调用评论接口 |
| **问题 2** | PostDetailScreen 评论发送按钮未绑定实际接口调用 |
| **问题 3** | PostDetailScreen 未展示评论列表（仅显示"暂无评论"占位） |
| 建议 | 1. 创建 `app/src/api/comment.ts`；2. 在 PostDetailScreen 中加载评论列表；3. 绑定发送按钮 |

---

### TC-CHAT-001/002: 私聊 ⚠️ 部分通过

| 检查项 | 结果 |
|---|---|
| 后端会话列表 | ✅ `/conversations` GET |
| 后端创建会话 | ✅ `/conversations` POST |
| 后端消息列表 | ✅ `/conversations/:id/messages` GET |
| 后端 WebSocket | ✅ 已实现消息广播 |
| App 聊天详情页 | ✅ ChatDetailScreen 已存在 |
| **问题 1** | App 端 **缺少 chat.ts API 文件** |
| **问题 2** | PostDetailScreen/PostCard 中无"私聊作者"入口 |
| **问题 3** | WebSocket token 验证硬编码 `userID := uint64(1)`，所有连接被视为同一用户 |
| 建议 | 1. 创建 `app/src/api/chat.ts`；2. 添加私聊入口按钮；3. 修复 WebSocket token 解析 |

---

### TC-REPORT-001: 举报帖子 ✅ Pass

| 检查项 | 结果 |
|---|---|
| 后端举报接口 | ✅ `/reports` POST |
| App 举报弹窗 | ✅ PostDetailScreen 已实现（6种原因 + 补充说明） |

**TC-REPORT-002: 举报评论** — 依赖评论列表展示，当前 Blocked。

---

### TC-MOD-001/002/003: 负责人删帖 ✅ Pass

| 检查项 | 结果 |
|---|---|
| 后端删帖接口 | ✅ `/moderation/posts/:id/delete`，含分区权限校验 |
| 后端删评论 | ✅ `/moderation/comments/:id/delete` |
| 后端举报列表 | ✅ `/moderation/categories/:id/reports` |
| 后端处理举报 | ✅ `/moderation/reports/:id/handle` |
| 后端操作日志 | ✅ `/moderation/logs` |
| App 管理面板 | ✅ ModeratorPanelScreen 已实现（处理举报、编辑公告/规则） |
| **问题** | 通知系统为空实现，删帖后作者收不到通知（已标记为已知限制） |

---

### TC-ELECTION-001~004: 选举 ✅ Pass

| 检查项 | 结果 |
|---|---|
| 后端选举列表 | ✅ `/elections` GET |
| 后端选举详情 | ✅ `/elections/:id` GET |
| 后端报名 | ✅ `/elections/:id/candidates` POST |
| 后端投票 | ✅ `/elections/:id/vote` POST |
| 后端结果 | ✅ `/elections/:id/result` GET |
| 后端结束任命 | ✅ `/admin/elections/:id/finish` POST（自动任命负责人/副负责人） |
| App 选举页面 | ✅ ElectionScreen 已实现（列表、详情、报名弹窗、投票按钮） |

---

### TC-ADMIN-001~004: 后台管理 ✅ Pass

| 检查项 | 结果 |
|---|---|
| 后台用户管理 | ✅ 列表、状态修改 |
| 后台分区管理 | ✅ 列表、创建、更新 |
| 后台帖子管理 | ✅ 列表、删除 |
| 后台负责人管理 | ✅ 列表、任命、撤销 |
| 后台选举管理 | ✅ 列表、创建、结束 |
| **问题** | 无只读管理员角色区分（role 仅 0/1 两种） |

---

## 4. 阻塞问题

| 编号 | 问题 | 影响 | 复现步骤 | 建议 |
|---|---|---|---|---|
| B-1 | App 端缺少 `comment.ts` API | 评论功能完全不可用 | 打开任意帖子详情，无法加载评论列表和发送评论 | 创建 `app/src/api/comment.ts`，在 PostDetailScreen 中接入 |
| B-2 | App 端缺少 `chat.ts` API | 私聊会话和消息列表无法调用 | 进入消息页面无法加载会话 | 创建 `app/src/api/chat.ts` |

---

## 5. 严重问题 (S2)

| 编号 | 问题 | 模块 | 复现步骤 | 实际结果 | 预期结果 |
|---|---|---|---|---|---|
| S2-1 | PostDetailScreen 缺少 Avatar 导入 | App | 编译时出错 | 编译失败：`Avatar is not defined` | 正常渲染头像 |
| S2-2 | report.ts 调用不存在的 `/reports/my` | App | 调用 `reportAPI.getMyReports()` | 404 错误 | 返回举报记录列表 |
| S2-3 | WebSocket 硬编码 userID | 后端 | 任意用户连接 WebSocket | 所有用户被视为 userID=1 | 根据 token 解析真实用户ID |
| S2-4 | Vote 票数更新非事务性 | 后端 | 并发投票 | 可能出现票数与投票记录不一致 | 数据一致 |
| S2-5 | Like 存在竞态条件 | 后端 | 并发点赞/取消点赞 | 可能重复点赞或数据不一致 | 数据一致 |

---

## 6. 一般问题 (S3)

| 编号 | 问题 | 模块 | 备注 |
|---|---|---|---|
| S3-1 | post.Delete 中无效代码 | 后端 | `now := config.Post{}.DeletedAt; _ = now` 无实际作用，应设置 `DeletedAt` 时间 |
| S3-2 | generateAvatarURL 返回空字符串 | 后端 | 注册时头像为空，依赖前端兜底 |
| S3-3 | PostCard 点赞按钮未绑定 | App | 信息流卡片上点赞仅展示，无法点击 |
| S3-4 | CategoryDetailScreen isModerator 判断依赖 moderators 加载时机 | App | 可能闪烁或判断错误 |
| S3-5 | 请求拦截器刷新 Token 时使用原始 axios | App | `axios.post` 未走统一拦截器，错误响应结构需小心处理 |

---

## 7. 必须修复清单

### 高优先级（阻塞测试）

1. **修复 PostDetailScreen 缺少 Avatar 导入**
   ```ts
   import Avatar from '../../components/Avatar';
   ```

2. **创建 `app/src/api/comment.ts`**
   ```ts
   export const commentAPI = {
     getList: (postId: number, limit?: number, offset?: number) =>
       request.get(`/posts/${postId}/comments`, { params: { limit, offset } }),
     create: (postId: number, content: string, parentId?: number) =>
       request.post(`/posts/${postId}/comments`, { content, parentId }),
     delete: (id: number) =>
       request.delete(`/comments/${id}`),
   };
   ```

3. **创建 `app/src/api/chat.ts`**
   ```ts
   export const chatAPI = {
     getConversations: () => request.get('/conversations'),
     createConversation: (targetUserId: number) =>
       request.post('/conversations', { targetUserId }),
     getMessages: (id: number, cursor?: string, limit?: number) =>
       request.get(`/conversations/${id}/messages`, { params: { cursor, limit } }),
   };
   ```

4. **修复 report.ts 中不存在的 `/reports/my`**
   - 方案 A：后端添加 `GET /reports/my` 接口
   - 方案 B：App 端移除 `getMyReports` 方法

### 中优先级（影响功能正确性）

5. **修复 WebSocket Token 验证**（`server/internal/modules/chat/router.go`）
   - 将硬编码 `userID := uint64(1)` 改为解析 JWT token

6. **将 Vote 票数更新放入事务**（`server/internal/modules/election/router.go`）
   - 使用 `db.Transaction` 包裹创建投票记录和更新票数

7. **将 Like 操作原子化**（`server/internal/modules/like/handler.go`）
   - 使用数据库唯一索引 + INSERT/DELETE 避免竞态

### 低优先级（体验优化）

8. **PostDetailScreen 接入评论列表和发送功能**
9. **PostCard 绑定点赞事件**
10. **generateAvatarURL 返回实际头像 URL**

---

## 8. 上线建议

- **是否可上线**：否
- **必须修复**：S2-1 ~ S2-3（编译错误、API 缺失、WebSocket 安全问题）
- **可延后修复**：S3 级别问题
- **下轮回归重点**：评论功能端到端、私聊功能端到端、图片上传功能
